apiVersion: apps/v1
kind: Deployment
metadata:
  name: production-{{cookiecutter.project_slug}}-backend-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      component: production-{{cookiecutter.project_slug}}-backend
  template:
    metadata:
      labels:
        component: production-{{cookiecutter.project_slug}}-backend
    spec:
      imagePullSecrets:
        - name: regcred
      volumes:
        - name: production-{{cookiecutter.project_slug}}-static-storage
          persistentVolumeClaim:
            claimName: production-{{cookiecutter.project_slug}}-static-persistent-volume-claim
        - name: production-{{cookiecutter.project_slug}}-media-storage
          persistentVolumeClaim:
            claimName: production-{{cookiecutter.project_slug}}-media-persistent-volume-claim
      containers:
        - name: backend
          image: registry.gitlab.com/{{cookiecutter.project_slug}}/{{cookiecutter.project_dirname}}:__IMAGE_TAG__
          ports:
            - containerPort: 8000
          volumeMounts:
            - name: production-{{cookiecutter.project_slug}}-static-storage
              mountPath: /app/static
              subPath: static
            - name: production-{{cookiecutter.project_slug}}-media-storage
              mountPath: /app/media
              subPath: media
          env:
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: production-{{cookiecutter.project_slug}}-secrets
                  key: POSTGRES_DB
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: production-{{cookiecutter.project_slug}}-secrets
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: production-{{cookiecutter.project_slug}}-secrets
                  key: POSTGRES_PASSWORD
            - name: DATABASE_URL
              value: postgres://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@production-{{cookiecutter.project_slug}}-postgres-cluster-ip-service:5432/$(POSTGRES_DB)
            - name: DJANGO_CONFIGURATION
              valueFrom:
                secretKeyRef:
                  name: development-nalablog-secrets
                  key: DJANGO_CONFIGURATION
            - name: DJANGO_ALLOWED_HOSTS
              valueFrom:
                secretKeyRef:
                  name: production-{{cookiecutter.project_slug}}-secrets
                  key: DJANGO_ALLOWED_HOSTS
            - name: DJANGO_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: production-{{cookiecutter.project_slug}}-secrets
                  key: DJANGO_SECRET_KEY
            - name: DJANGO_SERVER_EMAIL
              valueFrom:
                secretKeyRef:
                  name: production-{{cookiecutter.project_slug}}-secrets
                  key: DJANGO_SERVER_EMAIL
            - name: DJANGO_DEBUG
              valueFrom:
                secretKeyRef:
                  name: production-{{cookiecutter.project_slug}}-secrets
                  key: DJANGO_DEBUG
            - name: EMAIL_URL
              valueFrom:
                secretKeyRef:
                  name: production-{{cookiecutter.project_slug}}-secrets
                  key: EMAIL_URL

---

apiVersion: v1
kind: Service
metadata:
  name: production-{{cookiecutter.project_slug}}-backend-cluster-ip-service
spec:
  type: ClusterIP
  selector:
    component: production-{{cookiecutter.project_slug}}-backend
  ports:
    - port: 8000
      targetPort: 8000
